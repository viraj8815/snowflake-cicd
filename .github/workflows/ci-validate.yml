name: CI Pipeline - Stage Validation (Proper ENV Injection)

on:
  push:
    branches:
      - stage

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install schemachange
        run: pip install schemachange

      - name: Validate SQL Changes (Dry Run)
        run: |
          export SNOWFLAKE_ACCOUNT=gszncrk-nvb75074
          export SNOWFLAKE_USER=virajcom1
          export SNOWFLAKE_PASSWORD='Viraj@1234567890'
          export SNOWFLAKE_ROLE=accountadmin
          export SNOWFLAKE_WAREHOUSE=compute_wh
          export SNOWFLAKE_DATABASE=STAGE_DB
          export SNOWFLAKE_SCHEMA=PUBLIC

          schemachange deploy -f sql/ --create-change-history-table
