name: CI Pipeline - Stage Validation (with Hardcoded ENV Variables)

on:
  push:
    branches:
      - stage

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install schemachange
        run: pip install schemachange

      - name: Validate SQL Changes (Dry Run Style)
        run: |
          schemachange deploy -f sql/ --create-change-history-table
        env:
          SNOWFLAKE_ACCOUNT: gszncrk-nvb75074
          SNOWFLAKE_USER: virajcom1
          SNOWFLAKE_PASSWORD: Viraj@1234567890
          SNOWFLAKE_ROLE: accountadmin
          SNOWFLAKE_WAREHOUSE: compute_wh
          SNOWFLAKE_DATABASE: STAGE_DB
          SNOWFLAKE_SCHEMA: PUBLIC
