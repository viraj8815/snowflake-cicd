CREATE OR REPLACE VIEW CUSTOMERS_VIEW AS
SELECT
  CUSTOMERID,
  COMPANYNAME,
  CONTACTNAME,
  CONTACTTITLE,
  ADDRESS,
  CITY,
  REGION,
  POSTALCODE,
  COUNTRY,
  PHONE,
  FAX
FROM STAGE_DB.PUBLIC.CUSTOMERS;

